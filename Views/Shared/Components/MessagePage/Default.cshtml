@model PetShop.MessagePage.Message
@{
    Layout = "_Layout";
    ViewData["Title"] = @Model.title;
}
<div class="card m-4">
    <div class="card-header bg-success text-light">
        <b>@Model.title</b>
    </div>
    <div class="card-body">
        @Html.Raw(Model.htmlcontent)
    </div>
</div>